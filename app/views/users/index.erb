<h1>All of our wonderful clients</h1>

<ul>
   <% @users.each do |user| %>
<li><%= link_to user.full_name, user_path(user) %> - <%= pluralize(user.dog_count, 'dog') %></li>
     <% end %>

</ul>

<p><%=link_to "Add new User", {:action => 'new' }%></p>
